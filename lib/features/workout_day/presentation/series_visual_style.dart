/// Visual density for series rows inside an exercise card.
enum SeriesVisualStyle {
  /// Full form fields (default series outside technique blocks).
  standard,

  /// Compact execution log inside warmup/drop blocks.
  compactExecution,
}
